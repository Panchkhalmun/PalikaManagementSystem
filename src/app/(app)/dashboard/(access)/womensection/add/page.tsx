import AddDetails from "@/components/(app)/dashboard/(access)/womensection/AddDetails";
import React from "react";

const page = () => {
  return (
    <div>
      <AddDetails />
    </div>
  );
};

export default page;
